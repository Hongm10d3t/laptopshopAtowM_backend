# Copy file này thành .env rồi điền giá trị thật. Không commit file .env.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=laptophub
DB_USERNAME=root
DB_PASSWORD=198074

# Dùng ở giai đoạn security/auth (JWT signing key)
JWT_SECRET=changeme

# Dùng ở giai đoạn chatbot (tư vấn sản phẩm bằng AI)
OPENAI_API_KEY=changeme
